

<!DOCTYPE html>
<html lang="he" dir="rtl">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Luxury Sky Spa</title>
  <style>
    body {
      margin: 0;
      font-family: "Arial", sans-serif;
      background-color: #000;
      color: #fff;
      direction: rtl;
    }
    .slider {
      position: relative;
      width: 100%;
      max-width: 800px;
      margin: 0 auto;
      overflow: hidden;
      border-bottom: 4px solid #ffeb3b;
    }
    .slides {
      display: flex;
      transition: transform 0.5s ease-in-out;
      width: 300%;
    }
    .slides img {
      width: 100%;
      height: 400px;
      object-fit: cover;
    }
    .nav {
      position: absolute;
      width: 100%;
      top: 50%;
      display: flex;
      justify-content: space-between;
      transform: translateY(-50%);
      padding: 0 10px;
    }
    .nav button {
      background: rgba(0, 0, 0, 0.5);
      border: none;
      color: white;
      font-size: 2em;
      cursor: pointer;
    }
    .dots {
      text-align: center;
      margin-top: 10px;
    }
    .dot {
      display: inline-block;
      width: 12px;
      height: 12px;
      margin: 0 5px;
      background-color: #bbb;
      border-radius: 50%;
      cursor: pointer;
    }
    .dot.active {
      background-color: #ffeb3b;
    }
    .container {
      background-color: rgba(0, 0, 0, 0.7);
      padding: 30px;
      border-radius: 16px;
      max-width: 600px;
      margin: 30px auto;
      color: #fff;
      text-shadow: 1px 1px 4px #000;
    }
    h1 {
      font-size: 2.5em;
      color: #ffeb3b;
    }
    p {
      font-size: 1.2em;
      line-height: 1.8;
    }
    .button {
      display: inline-block;
      margin-top: 20px;
      padding: 12px 24px;
      font-size: 1.2em;
      background-color: #ffeb3b;
      color: #000;
      border: none;
      border-radius: 8px;
      text-decoration: none;
      transition: background-color 0.3s;
    }
    .button:hover {
      background-color: #fdd835;
    }
  </style>
</head>
<body>

  <div class="slider">
<div class="slides" id="slides">
      <img src="~/images/1eca68ae-214f-443b-a82f-b2a609e1211b.jpg alt="slide1"/>
      <img src="https://source.unsplash.com/800x400/?jacuzzi" alt="slide2"/>
      <img src="https://source.unsplash.com/800x400/?mountains,resort" alt="slide3"/>
    </div>
    <div class="nav">
      <button onclick="moveSlide(-1)">❮</button>
      <button onclick="moveSlide(1)">❯</button>
    </div>
    <div class="dots">
      <span class="dot" onclick="setSlide(0)"></span>
      <span class="dot" onclick="setSlide(1)"></span>
      <span class="dot" onclick="setSlide(2)"></span>
    </div>
  </div>

  <div class="container">
    <h1>Luxury Sky Spa</h1>
    <p>
      צימר זוגי עם ספא פרטי מול נוף הררי מרהיב — בהר שמואל ליד ירושלים.<br>
      בישוב הפסטורלי הר שמואל, למרגלות קבר שמואל הנביא ובמרחק דקות משכונת רמות בירושלים, מחכה לכם צימר מפנק ומרווח עם נוף עוצר נשימה.
      <br><br>
      <strong>במקום תיהנו מ:</strong><br>
      🌿 סאונה יבשה פרטית<br>
      🛁 ג'קוזי זוגי ענק על הגג<br>
      🧼 רמת ניקיון גבוהה מאוד<br>
      🛏️ חדר שינה מרווח ומאובזר עד הפרטים הקטנים<br>
      🧃 כיבוד עשיר: שתייה חמה וקרה, מגש פירות, בירות וחטיפים
      <br><br>
      מתאים במיוחד לזוגות שמחפשים שקט, שלווה וניתוק אמיתי מהשגרה.<br>
      פרטיות מוחלטת – גם אפשרות לאירוח עד 4 נפשות לפי שעה.
    </p>
    <a href="tel:0533369890" class="button">להזמנה ופרטים – 053-3369890</a>
  </div>

  <script>
    let currentIndex = 0;
    const slides = document.getElementById('slides');
    const dots = document.querySelectorAll('.dot');

    function updateSlider() {
      slides.style.transform = 'translateX(' + (-currentIndex * 100) + '%)';
      dots.forEach(dot => dot.classList.remove('active'));
      dots[currentIndex].classList.add('active');
    }

    function moveSlide(n) {
      currentIndex = (currentIndex + n + 3) % 3;
      updateSlider();
    }

    function setSlide(n) {
      currentIndex = n;
      updateSlider();
    }

    // Auto-slide
    setInterval(() => {
      moveSlide(1);
    }, 5000);

    updateSlider();
  </script>
</body>
</html>
